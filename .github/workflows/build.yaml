name: Build and Release

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-22.04
            platform: linux
            arch: x64
          - os: windows-latest
            platform: windows
            arch: x64
          - os: macos-latest
            platform: macos
            arch: arm64

    steps:
      - uses: actions/checkout@v4

      - name: Get version from package.json
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
        shell: bash

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: pip install -r requirements.txt pyinstaller

      - name: Install macOS build dependencies
        if: runner.os == 'macOS'
        run: |
          brew install xz pkg-config

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          npm install
          npm install @rollup/rollup-linux-x64-gnu sqlite3 --save-optional
        env:
          npm_config_optional: true

      - name: Install dependencies (Windows x64)
        if: runner.os == 'Windows' && matrix.arch == 'x64'
        run: |
          npm cache clean --force
          npm install
          npm install @rollup/rollup-win32-x64-msvc sqlite3 --save
          npm install node-pty@beta --save-optional --ignore-scripts
        env:
          npm_config_optional: true

      - name: Install dependencies (macOS ARM64)
        if: runner.os == 'macOS' && matrix.arch == 'arm64'
        run: |
          npm install
          npm install @rollup/rollup-darwin-arm64 sqlite3 --save
        env:
          npm_config_optional: true

      - name: Build PyInstaller binary (Linux)
        if: runner.os == 'Linux'
        run: |
          pyinstaller --onefile --clean --distpath pyinstaller_dist --noupx \
            --hidden-import=npcpy --hidden-import=npcsh --hidden-import=litellm --hidden-import=ollama \
            --hidden-import=flask --hidden-import=flask_cors --hidden-import=flask_sse \
            --hidden-import=redis --hidden-import=pyyaml --hidden-import=pillow \
            --hidden-import=nltk --hidden-import=anthropic --hidden-import=openai \
            --hidden-import=google-genai --hidden-import=tiktoken_ext.openai_public \
            --hidden-import=tiktoken_ext --hidden-import=chromadb --hidden-import=pydantic \
            --exclude-module=scipy --exclude-module=tensorflow --exclude-module=torch \
            --exclude-module=sklearn --exclude-module=notebook --exclude-module=ipython \
            --exclude-module=jupyter --exclude-module=nbconvert --exclude-module=cv2 \
            --exclude-module=PIL.ImageTk --exclude-module=PIL.ImageQt --exclude-module=docx \
            --exclude-module=pptx --exclude-module=cuda --exclude-module=cudnn \
            --exclude-module=cudart --exclude-module=cublas --exclude-module=cupy \
            --exclude-module=logfire --exclude-module=numba.cuda --exclude-module=torch.cuda \
            --exclude-module=tensorflow.python.framework.cuda_util \
            --collect-data=litellm --collect-data=npcpy --collect-data=npcsh incognide_serve.py
          mkdir -p resources/backend
          cp pyinstaller_dist/incognide_serve resources/backend/
          chmod +x resources/backend/incognide_serve

      - name: Build PyInstaller binary (Windows)
        if: runner.os == 'Windows'
        run: |
          python -m PyInstaller --onefile --clean --distpath pyinstaller_dist --noupx --hidden-import=npcpy --hidden-import=npcsh --hidden-import=flask --hidden-import=flask_cors --hidden-import=flask_sse --hidden-import=redis --hidden-import=pyyaml --hidden-import=pillow --hidden-import=nltk --hidden-import=litellm --hidden-import=anthropic --hidden-import=openai --hidden-import=google-genai --hidden-import=tiktoken_ext.openai_public --hidden-import=tiktoken_ext --hidden-import=sentence_transformers --hidden-import=chromadb --hidden-import=pydantic --collect-data=litellm --collect-data=npcpy --collect-data=npcsh incognide_serve.py
          mkdir -p resources/backend
          copy pyinstaller_dist\incognide_serve.exe resources\backend\incognide_serve.exe

      - name: Build PyInstaller binary (macOS)
        if: runner.os == 'macOS'
        run: |
          pyinstaller --onefile --clean --distpath pyinstaller_dist --noupx \
            --hidden-import=npcpy --hidden-import=npcsh --hidden-import=litellm --hidden-import=ollama \
            --hidden-import=flask --hidden-import=flask_cors --hidden-import=flask_sse \
            --hidden-import=redis --hidden-import=pyyaml --hidden-import=pillow \
            --hidden-import=anthropic --hidden-import=openai --hidden-import=google-genai \
            --hidden-import=tiktoken_ext.openai_public --hidden-import=tiktoken_ext \
            --hidden-import=chromadb --hidden-import=pydantic \
            --exclude-module=scipy --exclude-module=tensorflow --exclude-module=torch \
            --exclude-module=sklearn --exclude-module=notebook --exclude-module=ipython \
            --exclude-module=jupyter --exclude-module=nbconvert --exclude-module=cv2 \
            --exclude-module=PIL.ImageTk --exclude-module=PIL.ImageQt --exclude-module=docx \
            --exclude-module=pptx --exclude-module=cuda --exclude-module=cudnn \
            --exclude-module=cudart --exclude-module=cublas --exclude-module=cupy \
            --exclude-module=logfire --exclude-module=numba.cuda --exclude-module=torch.cuda \
            --exclude-module=nltk --exclude-module=tensorflow.python.framework.cuda_util \
            --collect-data=litellm --collect-data=npcpy --collect-data=npcsh incognide_serve.py
          mkdir -p resources/backend
          cp pyinstaller_dist/incognide_serve resources/backend/incognide_serve
          chmod +x resources/backend/incognide_serve

      - name: Build frontend
        run: npm run build:vite
        env:
          VITE_CLERK_PUBLISHABLE_KEY: ${{ secrets.VITE_CLERK_PUBLISHABLE_KEY }}

      - name: Rebuild native modules for Electron (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          npx electron-rebuild --force --module-dir . --which-module sqlite3
          npx electron-rebuild --force --module-dir . --which-module node-pty || true
        env:
          npm_config_build_from_source: true

      - name: Rebuild native modules for Electron (Windows)
        if: runner.os == 'Windows'
        run: |
          npx electron-rebuild --force --module-dir . --which-module sqlite3
          npx electron-rebuild --force --module-dir . --which-module node-pty || true

      - name: Import Apple Developer ID certificate and set signing environment
        if: runner.os == 'macOS'
        env:
          P12_PASSWORD: ${{ secrets.APPLE_DEVELOPER_ID_CERTIFICATE_PASSWORD }}
        run: |
          # Decode the certificate and save it
          echo "${{ secrets.APPLE_DEVELOPER_ID_APPLICATION }}" | base64 --decode > build_cert.p12

          # Create a temporary keychain
          security create-keychain -p "$P12_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$P12_PASSWORD" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain

          # Import the certificate into the keychain
          security import build_cert.p12 -k build.keychain -P "$P12_PASSWORD" -T /usr/bin/codesign -T /usr/bin/productsign

          # Allow access to the certificate for code signing tools
          security set-key-partition-list -S apple-tool:,apple: -s -k "$P12_PASSWORD" build.keychain

          # Set environment variables for electron-builder signing
          echo "CSC_LINK=$(pwd)/build_cert.p12" >> $GITHUB_ENV
          echo "CSC_KEY_PASSWORD=$P12_PASSWORD" >> $GITHUB_ENV

      - name: Sign the backend binary (macOS)
        if: runner.os == 'macOS'
        run: |
          # Sign the PyInstaller backend binary with hardened runtime before packaging
          if [ -f "resources/backend/incognide_serve" ]; then
            codesign --force --options runtime \
              --entitlements resources/entitlements.mac.inherit.plist \
              --sign "Developer ID Application" \
              resources/backend/incognide_serve
            echo "Backend binary signed successfully"
            codesign --verify --verbose resources/backend/incognide_serve
          fi

      - name: Build Electron app (DMG + ZIP for direct distribution)
        run: npm run electron:build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TARGET_ARCH: ${{ matrix.arch }}
          # Notarization credentials (macOS only — electron-builder ignores on other platforms)
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

      - name: Verify notarization (macOS)
        if: runner.os == 'macOS'
        run: |
          DMG_FILE=$(find dist-electron -name "*.dmg" | head -1)
          if [ -n "$DMG_FILE" ]; then
            echo "Verifying notarization for: $DMG_FILE"
            spctl --assess --type open --context context:primary-signature "$DMG_FILE" || echo "Warning: notarization verification returned non-zero (may need stapling)"
            codesign --verify --verbose=2 "$DMG_FILE" || true
          fi

      - name: Prepare files for upload
        id: prepare
        run: |
          mkdir -p upload
          VERSION="${{ steps.version.outputs.version }}"

          if [ "${{ runner.os }}" = "Linux" ]; then
            find dist-electron -name "*.deb" -exec cp {} upload/incognide_${VERSION}_${{ matrix.arch }}.deb \;
            find dist-electron -name "*.AppImage" -exec cp {} upload/incognide_${VERSION}_${{ matrix.arch }}.AppImage \;
            echo "files=upload/incognide_${VERSION}_${{ matrix.arch }}.deb" >> $GITHUB_OUTPUT
            echo "gcs_path=linux-${{ matrix.arch }}" >> $GITHUB_OUTPUT
          elif [ "${{ runner.os }}" = "Windows" ]; then
            find dist-electron -maxdepth 1 -name "*.exe" -exec cp {} upload/incognide_${VERSION}_${{ matrix.arch }}.exe \;
            echo "files=upload/incognide_${VERSION}_${{ matrix.arch }}.exe" >> $GITHUB_OUTPUT
            echo "gcs_path=windows-${{ matrix.arch }}" >> $GITHUB_OUTPUT
          elif [ "${{ runner.os }}" = "macOS" ]; then
            find dist-electron -name "*.dmg" -exec cp {} upload/incognide_${VERSION}_${{ matrix.arch }}.dmg \;
            # Also grab the zip for auto-update support
            find dist-electron -name "*.zip" -exec cp {} upload/incognide_${VERSION}_${{ matrix.arch }}.zip \; 2>/dev/null || true
            echo "files=upload/incognide_${VERSION}_${{ matrix.arch }}.dmg" >> $GITHUB_OUTPUT
            echo "gcs_path=macos-${{ matrix.arch }}" >> $GITHUB_OUTPUT
          fi
        shell: bash

      - name: Authenticate to Google Cloud
        if: github.ref == 'refs/heads/main'
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        if: github.ref == 'refs/heads/main'
        uses: google-github-actions/setup-gcloud@v2

      - name: Upload to Google Cloud Storage
        if: github.ref == 'refs/heads/main'
        run: |
          for file in upload/*; do
            if [ -f "$file" ]; then
              filename=$(basename "$file")
              gsutil cp "$file" gs://incognide-executables/${{ steps.prepare.outputs.gcs_path }}/"$filename"
              echo "Uploaded $filename to gs://incognide-executables/${{ steps.prepare.outputs.gcs_path }}/"
            fi
          done
        shell: bash

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: incognide-${{ matrix.platform }}-${{ matrix.arch }}
          path: upload/*
          retention-days: 30

  # Mac App Store build
  build-mas:
    if: github.ref == 'refs/heads/main'
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Get version from package.json
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: pip install -r requirements.txt pyinstaller

      - name: Install macOS build dependencies
        run: brew install xz pkg-config

      - name: Install dependencies (macOS ARM64)
        run: |
          npm install
          npm install @rollup/rollup-darwin-arm64 sqlite3 --save
        env:
          npm_config_optional: true

      - name: Build PyInstaller binary (macOS)
        run: |
          pyinstaller --onefile --clean --distpath pyinstaller_dist --noupx \
            --hidden-import=npcpy --hidden-import=npcsh --hidden-import=litellm --hidden-import=ollama \
            --hidden-import=flask --hidden-import=flask_cors --hidden-import=flask_sse \
            --hidden-import=redis --hidden-import=pyyaml --hidden-import=pillow \
            --hidden-import=anthropic --hidden-import=openai --hidden-import=google-genai \
            --hidden-import=tiktoken_ext.openai_public --hidden-import=tiktoken_ext \
            --hidden-import=chromadb --hidden-import=pydantic \
            --exclude-module=scipy --exclude-module=tensorflow --exclude-module=torch \
            --exclude-module=sklearn --exclude-module=notebook --exclude-module=ipython \
            --exclude-module=jupyter --exclude-module=nbconvert --exclude-module=cv2 \
            --exclude-module=PIL.ImageTk --exclude-module=PIL.ImageQt --exclude-module=docx \
            --exclude-module=pptx --exclude-module=cuda --exclude-module=cudnn \
            --exclude-module=cudart --exclude-module=cublas --exclude-module=cupy \
            --exclude-module=logfire --exclude-module=numba.cuda --exclude-module=torch.cuda \
            --exclude-module=nltk --exclude-module=tensorflow.python.framework.cuda_util \
            --collect-data=litellm --collect-data=npcpy --collect-data=npcsh incognide_serve.py
          mkdir -p resources/backend
          cp pyinstaller_dist/incognide_serve resources/backend/incognide_serve
          chmod +x resources/backend/incognide_serve

      - name: Build frontend
        run: npm run build:vite
        env:
          VITE_CLERK_PUBLISHABLE_KEY: ${{ secrets.VITE_CLERK_PUBLISHABLE_KEY }}

      - name: Rebuild native modules for Electron
        run: |
          npx electron-rebuild --force --module-dir . --which-module sqlite3
          npx electron-rebuild --force --module-dir . --which-module node-pty || true

      - name: Import MAS certificates and provisioning profile
        env:
          P12_PASSWORD: ${{ secrets.APPLE_MAS_CERTIFICATE_PASSWORD }}
        run: |
          # Create temporary keychain
          security create-keychain -p "$P12_PASSWORD" mas_build.keychain
          security default-keychain -s mas_build.keychain
          security unlock-keychain -p "$P12_PASSWORD" mas_build.keychain
          security set-keychain-settings -t 3600 -u mas_build.keychain
          security list-keychains -d user -s mas_build.keychain login.keychain-db

          # Import 3rd Party Mac Developer Application certificate (for signing the app)
          echo "${{ secrets.APPLE_MAS_APPLICATION_CERT }}" | base64 --decode > mas_app_cert.p12
          security import mas_app_cert.p12 -k mas_build.keychain -P "$P12_PASSWORD" -A

          # Import 3rd Party Mac Developer Installer certificate (for signing the pkg)
          echo "${{ secrets.APPLE_MAS_INSTALLER_CERT }}" | base64 --decode > mas_installer_cert.p12
          if [ ! -s mas_installer_cert.p12 ]; then
            echo "ERROR: APPLE_MAS_INSTALLER_CERT secret is empty or not set"
            exit 1
          fi
          # Show what's in the installer cert
          echo "=== Installer cert CN ==="
          openssl pkcs12 -in mas_installer_cert.p12 -passin pass:"$P12_PASSWORD" -nokeys -clcerts 2>/dev/null | openssl x509 -noout -subject 2>/dev/null || echo "Could not read cert subject"
          security import mas_installer_cert.p12 -k mas_build.keychain -P "$P12_PASSWORD" -A

          # Allow access to all imported certs
          security set-key-partition-list -S apple-tool:,apple:,productbuild: -s -k "$P12_PASSWORD" mas_build.keychain

          # Install provisioning profile
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          echo "${{ secrets.APPLE_MAS_PROVISIONING_PROFILE }}" | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/incognide_mas.provisionprofile
          echo "${{ secrets.APPLE_MAS_PROVISIONING_PROFILE }}" | base64 --decode > incognide_mas.provisionprofile

          echo "CSC_INSTALLER_LINK=$(pwd)/mas_installer_cert.p12" >> $GITHUB_ENV
          echo "CSC_INSTALLER_KEY_PASSWORD=$P12_PASSWORD" >> $GITHUB_ENV

          # Verify what identities are actually in the keychain
          echo "=== All identities in keychain ==="
          security find-identity -v mas_build.keychain
          echo "=== Installer identity check ==="
          security find-identity -v mas_build.keychain | grep -i "installer" || echo "FATAL: No installer identity found. The APPLE_MAS_INSTALLER_CERT secret does not contain a '3rd Party Mac Developer Installer' certificate."

      - name: Sign the backend binary for MAS
        run: |
          if [ -f "resources/backend/incognide_serve" ]; then
            codesign --force --options runtime \
              --entitlements resources/entitlements.mas.inherit.plist \
              --sign "Apple Distribution" \
              resources/backend/incognide_serve
            echo "Backend binary signed for MAS"
            codesign --verify --verbose resources/backend/incognide_serve
          fi

      - name: Build MAS package
        run: |
          rm -rf dist-electron
          sed -i '' 's/findIdentity)(certType, masOptions.identity,/findIdentity)(certType, null,/' node_modules/app-builder-lib/out/macPackager.js
          npx electron-builder --mac mas --arm64
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

      - name: Prepare MAS artifact
        id: prepare_mas
        run: |
          mkdir -p upload
          VERSION="${{ steps.version.outputs.version }}"
          find dist-electron -name "*.pkg" -exec cp {} upload/incognide_${VERSION}_mas.pkg \;
          echo "pkg_file=upload/incognide_${VERSION}_mas.pkg" >> $GITHUB_OUTPUT

      - name: Upload MAS artifact
        uses: actions/upload-artifact@v4
        with:
          name: incognide-mas-arm64
          path: upload/*.pkg
          retention-days: 30

      - name: Upload to App Store Connect via Transporter
        run: |
          PKG_FILE="${{ steps.prepare_mas.outputs.pkg_file }}"
          if [ -f "$PKG_FILE" ]; then
            xcrun altool --upload-app \
              --type macos \
              --file "$PKG_FILE" \
              --username "${{ secrets.APPLE_ID }}" \
              --password "${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}" \
              --asc-provider "${{ secrets.APPLE_TEAM_ID }}" || echo "Warning: App Store upload failed. You can upload manually via Transporter app."
          fi

  update_manifest:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4
      - name: Get version from package.json
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Update manifest
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          cat > manifest.json << EOF
          {
            "version": "$VERSION",
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "downloads": {
              "linux-x64": "https://storage.googleapis.com/incognide-executables/linux-x64/incognide_${VERSION}_x64.deb",
              "linux-x64-appimage": "https://storage.googleapis.com/incognide-executables/linux-x64/incognide_${VERSION}_x64.AppImage",
              "windows-x64": "https://storage.googleapis.com/incognide-executables/windows-x64/incognide_${VERSION}_x64.exe",
              "macos-arm64": "https://storage.googleapis.com/incognide-executables/macos-arm64/incognide_${VERSION}_arm64.dmg"
            }
          }
          EOF
          gsutil cp manifest.json gs://incognide-executables/manifest.json
          gsutil setmeta -h "Cache-Control:no-cache,max-age=0" gs://incognide-executables/manifest.json

  notify:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4
      - name: Get version from package.json
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create download links summary
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          echo "## New incognide build available!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** $VERSION" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Download links:" >> $GITHUB_STEP_SUMMARY
          echo "- **Linux x64 (deb):** https://storage.googleapis.com/incognide-executables/linux-x64/incognide_${VERSION}_x64.deb" >> $GITHUB_STEP_SUMMARY
          echo "- **Linux x64 (AppImage):** https://storage.googleapis.com/incognide-executables/linux-x64/incognide_${VERSION}_x64.AppImage" >> $GITHUB_STEP_SUMMARY
          echo "- **Windows x64:** https://storage.googleapis.com/incognide-executables/windows-x64/incognide_${VERSION}_x64.exe" >> $GITHUB_STEP_SUMMARY
          echo "- **macOS ARM64:** https://storage.googleapis.com/incognide-executables/macos-arm64/incognide_${VERSION}_arm64.dmg" >> $GITHUB_STEP_SUMMARY
